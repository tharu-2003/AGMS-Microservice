server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/agms_automation_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=Asia/Colombo
    username: root
    password: Ijse@1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: health,info