server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/agms_sensor_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=Asia/Colombo
    username: root
    password: Ijse@1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

external:
  iot:
    base-url: http://104.211.95.241:8080/api
    username: greenhouse_user
    password: 123456

automation:
  service-url: http://AUTOMATION-SERVICE

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: health,info