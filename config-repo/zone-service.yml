server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/agms_zone_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=Asia/Colombo
    username: root
    password: Ijse@1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

external:
  iot:
    base-url: http://104.211.95.241:8080/api

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: health,info